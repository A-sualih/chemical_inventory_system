# Local Vite proxy target (dev only)
VITE_API_PROXY_TARGET=https://chemical-inventory-system-6x49.onrender.com

# Do NOT set VITE_API_ORIGIN in local .env unless you want the browser
# to call Render directly during `npm run dev`.
# Production uses frontend/.env.production
